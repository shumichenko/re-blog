<?php

namespace App\Api\Service\Article;

use App\Api\Entity\Article;
use App\Api\Service\BasicService;

class ArticleUploader extends BasicService
{
    public function uploadArticle()
    {/*
        $article = new Article();
        $article->setTitle('My first article');
        $article->setContent('So, this is really kinda of my first article here.');
        $article->setImageLink('kek');
        $article->setAppearanceStatus(1);
        $article->setCreatedAt(new \DateTime("now"));
        $article->setUpdatedAt(new \DateTime("now"));
        $article->setAuthor($user);
        $this->entityManager->persist($user);
        $this->entityManager->persist($article);
        $this->entityManager->flush();*/
    }
}
