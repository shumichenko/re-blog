<?php

namespace App\Api\Service\Domain;

use App\Api\Entity\Project;
use App\Api\Service\Domain\DomainService;

class ProjectUploader extends DomainService
{
    public function uploadProject()
    {/*
        $project = new Project();
        $project->setTitle('My first project');
        $project->setContent('So, this is really kinda of my first project here.');
        $project->setImageLink('kek');
        $project->setAppearanceStatus(1);
        $project->setCreatedAt(new \DateTime("now"));
        $project->setUpdatedAt(new \DateTime("now"));
        $project->setAuthor($user);
        $this->entityManager->persist($user);
        $this->entityManager->persist($project);
        $this->entityManager->flush();*/
    }
}
